spring:
  profiles: test
  datasource:
    generate-unique-name: true
  jpa:
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  jackson:
    default-property-inclusion: non_null
logging:
  level:
    com.social.backend: debug
