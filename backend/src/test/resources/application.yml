spring:
  profiles: test
  datasource:
    generate-unique-name: true
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  jackson:
    default-property-inclusion: non_null
management:
  endpoints:
    enabled-by-default: false
